# Animation: fireflies

**Implemented in v1.2.0 Story Engine** — see `FirefliesOverlay` in this
folder (`fireflies_overlay.dart`), registered in
`AnimationOverlayFactory` against `AnimationType.fireflies`.
