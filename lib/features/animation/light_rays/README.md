# Animation: light_rays

**Implemented in v1.2.0 Story Engine** — see `LightRaysOverlay` in this
folder (`light_rays_overlay.dart`), registered in
`AnimationOverlayFactory` against `AnimationType.lightRays`.
